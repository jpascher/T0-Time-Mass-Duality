# Branch Closing Workflow / Workflow zum Schließen von Branches

## 🇩🇪 Deutsch: Empfohlener Workflow

```
┌─────────────────────────────────────────────────────────────┐
│  Schritt 1: Vorbereitung                                    │
├─────────────────────────────────────────────────────────────┤
│  ☐ Wechseln Sie zum main Branch: git checkout main         │
│  ☐ Aktualisieren Sie main: git pull origin main            │
│  ☐ Wichtige Änderungen sichern                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Schritt 2: Branches überprüfen                            │
├─────────────────────────────────────────────────────────────┤
│  ☐ Alle Branches anzeigen: git branch -a                   │
│  ☐ Gemergede Branches: git branch --merged                 │
│  ☐ Nicht gemergede: git branch --no-merged                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Schritt 3: Entscheiden                                    │
├─────────────────────────────────────────────────────────────┤
│  Option A: Einzelne Branches löschen                       │
│  Option B: Alle Branches auf einmal löschen                │
│  Option C: Nur gemergede Branches löschen                  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Schritt 4: Ausführen                                      │
├─────────────────────────────────────────────────────────────┤
│  Methode 1: Manuell mit Git-Befehlen                       │
│  Methode 2: Mit interaktivem Skript                        │
│    - Windows: close_branches.bat                           │
│    - Linux/Mac: ./close_branches.sh                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Schritt 5: Verifizieren                                   │
├─────────────────────────────────────────────────────────────┤
│  ☐ Prüfen: git branch -a                                   │
│  ☐ Aufräumen: git fetch --prune                            │
│  ☐ Remote prüfen: git remote prune origin                  │
└─────────────────────────────────────────────────────────────┘
```

## 🇬🇧 English: Recommended Workflow

```
┌─────────────────────────────────────────────────────────────┐
│  Step 1: Preparation                                        │
├─────────────────────────────────────────────────────────────┤
│  ☐ Switch to main branch: git checkout main                │
│  ☐ Update main: git pull origin main                       │
│  ☐ Backup important changes                                │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Review branches                                   │
├─────────────────────────────────────────────────────────────┤
│  ☐ Show all branches: git branch -a                        │
│  ☐ Merged branches: git branch --merged                    │
│  ☐ Unmerged: git branch --no-merged                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 3: Decide                                            │
├─────────────────────────────────────────────────────────────┤
│  Option A: Delete individual branches                      │
│  Option B: Delete all branches at once                     │
│  Option C: Delete only merged branches                     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: Execute                                           │
├─────────────────────────────────────────────────────────────┤
│  Method 1: Manually with Git commands                      │
│  Method 2: With interactive script                         │
│    - Windows: close_branches.bat                           │
│    - Linux/Mac: ./close_branches.sh                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Verify                                            │
├─────────────────────────────────────────────────────────────┤
│  ☐ Check: git branch -a                                    │
│  ☐ Clean up: git fetch --prune                             │
│  ☐ Check remote: git remote prune origin                   │
└─────────────────────────────────────────────────────────────┘
```

## Häufige Szenarien / Common Scenarios

### 🇩🇪 Szenario 1: Nach dem Release aufräumen
```bash
# 1. Zu main wechseln
git checkout main

# 2. Alle Feature-Branches löschen
git branch | grep "feature/" | xargs git branch -D

# 3. Remote aufräumen
git branch -r | grep "feature/" | sed 's/origin\///' | xargs -I {} git push origin --delete {}
```

### 🇬🇧 Scenario 1: Clean up after release
```bash
# 1. Switch to main
git checkout main

# 2. Delete all feature branches
git branch | grep "feature/" | xargs git branch -D

# 3. Clean up remote
git branch -r | grep "feature/" | sed 's/origin\///' | xargs -I {} git push origin --delete {}
```

---

### 🇩🇪 Szenario 2: Nur gemergede Branches löschen
```bash
# Sicherer Ansatz - löscht nur gemergede Branches
git branch --merged main | grep -v "main" | xargs git branch -d
```

### 🇬🇧 Scenario 2: Delete only merged branches
```bash
# Safe approach - deletes only merged branches
git branch --merged main | grep -v "main" | xargs git branch -d
```

---

### 🇩🇪 Szenario 3: Einzelnen Branch vollständig entfernen
```bash
# 1. Lokal löschen
git branch -D branch-name

# 2. Remote löschen
git push origin --delete branch-name

# 3. Lokale Referenzen aufräumen
git fetch --prune
```

### 🇬🇧 Scenario 3: Completely remove a single branch
```bash
# 1. Delete locally
git branch -D branch-name

# 2. Delete remote
git push origin --delete branch-name

# 3. Clean up local references
git fetch --prune
```

## Sicherheitstipps / Safety Tips

### 🇩🇪 Deutsch
1. **Immer Backup erstellen** vor Massen-Löschungen
2. **Merge-Status prüfen** mit `git branch --merged`
3. **Dry-Run verwenden** - erst mit `echo` testen
4. **Main/Master schützen** - niemals löschen
5. **Team informieren** bei Remote-Änderungen

### 🇬🇧 English
1. **Always create backup** before bulk deletions
2. **Check merge status** with `git branch --merged`
3. **Use dry-run** - test with `echo` first
4. **Protect main/master** - never delete
5. **Inform team** for remote changes

## Weitere Ressourcen / Additional Resources

- 📖 [Vollständige Anleitung (DE)](BRANCH_MANAGEMENT_DE.md)
- 📖 [Complete Guide (EN)](BRANCH_MANAGEMENT_EN.md)
- 🚀 [Schnellreferenz / Quick Reference](QUICK_REFERENCE_BRANCHES.md)

---

**Fragen? / Questions?**  
**E-Mail**: johann.pascher@gmail.com
