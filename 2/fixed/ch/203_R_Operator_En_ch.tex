% ============================================================
% 203_R_Operator_En_ch.tex
% Der Rekursionsoperator R in FFGFT
% Formale Übersetzung der Derivation aus Dok. 180
% Johann Pascher — May 2026
% ============================================================

\section*{Abstract}

Der Rekursionsoperator $\mathcal{R}$ der FFGFT formalisiert die
fraktale Skalierungsstruktur im Mode space. Dieses Document übersetzt
die in Doc.~180 physikalisch hergeleitete Konstante
$\xi_0 = 4/30000$ in die formale Sprache des Operators und zeigt,
dass die Fixed pointgleichung $\mathcal{R}(\Phi_*) = \Phi_*$ genau
diese Eindeutigkeit reproduziert.

%------------------------------------------------------------
\section{Starting point: Die physikalische Derivation (Doc.~180)}
%------------------------------------------------------------

Doc.~180 leitet $\xi_0 = 4/30000$ aus drei unabhängigen Conditionen her:

\begin{enumerate}
  \item \textbf{Dimension contribution $10^{-4}$:}
    Vier Spacetime-Dimensionen liefern $\xi \sim (10^{-1})^4 = 10^{-4}$.
  \item \textbf{Geometrybeitrag $4/3$:}
    Tetrahedral symmetry: $4$ Ecken / $3$ edges per vertex $= 4/3$.
  \item \textbf{Self-consistency:}
    Das e-p-$\mu$-System liefert $\kappa = 7$ als einzige
    consistente Rekursionstiefe ($0{,}04\,\%$ Genauigkeit).
\end{enumerate}

Diese drei Conditionen legen $\xi_0$ ohne freien Parameter fest.
Die Task dieses Documents: Übersetzung in die $\mathcal{R}$-Sprache.

%------------------------------------------------------------
\section{Definition des Rekursionsoperators}
%------------------------------------------------------------

\subsection{Diagonale Form im Mode space}

Der Rekursionsoperator $\mathcal{R}$ wirkt auf den Hilbertraum
$\mathcal{H} = \bigoplus_{n,l,j}\mathbb{C}\cdot\mathbf{e}_{n,l,j}$
diagonal:
\begin{equation}
  \mathcal{R}(\mathbf{e}_{n,l,j})
  = \lambda_{n,l,j} \cdot \mathbf{e}_{n,l,j}
  \label{eq:R-diagonal}
\end{equation}

Die Eigenvaluee $\lambda_{n,l,j}$ sind durch die Massenformel bestimmt:
\begin{equation}
  \lambda_{n,l,j} = \xi_0^{p(n,l,j)-1} \cdot K_{\text{frak}}
  \label{eq:eigenwerte}
\end{equation}

mit $K_{\text{frak}} \approx 0{,}986$ (fraktale correction).

\subsection{Wirkung auf den Feldoperator}

Auf dem Moden-Operator $\Phi = \sum_i m_i P_i$ (Doc.~202) wirkt
$\mathcal{R}$ als Skalierungsabbildung:
\begin{equation}
  \mathcal{R}(\Phi) = \xi_0 \cdot \Phi
  \label{eq:R-auf-Phi}
\end{equation}

Das entspricht der Zeit-Masse-Dualitäts-Symmetrie:
\begin{equation}
  t \to \xi_0 \cdot t, \qquad m \to \xi_0^{-1} \cdot m,
  \qquad \Phi \to \Phi
  \label{eq:tmd-symmetrie}
\end{equation}

%------------------------------------------------------------
\section{Die Fixed pointgleichung}
%------------------------------------------------------------

\subsection{Definition des Fixed points}

Der Fixed point $\Phi_*$ ist der Operator, der unter $\mathcal{R}$
invariant ist:
\begin{equation}
  \mathcal{R}(\Phi_*) = \Phi_*
  \label{eq:fixpunkt}
\end{equation}

Aus \eqref{eq:R-auf-Phi} folgt direkt:
\begin{equation}
  \xi_0 \cdot \Phi_* = \Phi_*
  \quad \Rightarrow \quad
  (\xi_0 - 1)\,\Phi_* = 0
\end{equation}

Da $\xi_0 \neq 1$, folgt $\Phi_* = 0$ — ein trivialer Fixed point.

\subsection{Der nicht-triviale Fixed point durch Self-consistency}

Der physically relevant Fixed point entsteht durch die
Self-consistencybedingung der Massenformel. Der Operator
$\Phi_* = \sum_i m_i^{(*)} P_i$ ist ein Fixed point von $\mathcal{R}$
wenn gilt:
\begin{equation}
  m_i^{(*)} = r_i \cdot \xi_0^{p_i} \cdot v
  \quad \text{und} \quad
  \mathcal{R}(m_i^{(*)}) = m_i^{(*)}
  \label{eq:selbstkonsistenz}
\end{equation}

Die zweite Condition bedeutet:
\begin{equation}
  \xi_0^{p_i - 1} \cdot K_{\text{frak}} \cdot m_i^{(*)}
  = m_i^{(*)}
  \quad \Rightarrow \quad
  \xi_0^{p_i - 1} \cdot K_{\text{frak}} = 1
  \label{eq:fixpunkt-bedingung}
\end{equation}

\subsection{Auflösung nach \texorpdfstring{$\xi_0$}{xi0}}

Aus \eqref{eq:fixpunkt-bedingung} folgt für jede Mode $i$:
\begin{equation}
  \xi_0 = K_{\text{frak}}^{1/(p_i-1)}
  \label{eq:xi0-aus-fixpunkt}
\end{equation}

Damit $\xi_0$ \textbf{universell} (modunabhängig) ist, muss diese
Gleichung für alle Moden gleichzeitig erfüllbar sein. Das ist genau
die Self-consistencybedingung aus Doc.~180.

%------------------------------------------------------------
\section{Verbindung zu den drei Conditionen aus Doc.~180}
%------------------------------------------------------------

\subsection{Schritt 1: Dimension contribution}

Der Rekursionsoperator skaliert Längen mit $\xi_0$. In $D=4$
Spacetime-Dimensionen skaliert das 4D-Volumen mit $\xi_0^4$:
\begin{equation}
  \mathcal{R}^{(4D)}(V_4) = \xi_0^4 \cdot V_4
\end{equation}

Für den Massenoperator, der mit $\ell^{-1}$ skaliert:
\begin{equation}
  \xi_0^{\text{dim}} = (10^{-1})^4 = 10^{-4}
\end{equation}

\subsection{Schritt 2: Geometryfaktor}

Die Tetrahedral symmetry des 4D-Torus liefert den geometrischen Faktor
aus der Topologie der Flächen:

\begin{center}
{\footnotesize
\begin{tabular}{lcl}
\toprule
\textbf{Geometry} & \textbf{Value} & \textbf{Origin} \\
\midrule
Tetrahedron vertices & 4 & Punktsymmetrie \\
edges per vertex & 3 & Raumeinbettung \\
Geometryfaktor $\xi_0^{\text{geo}}$ & $4/3$ & Ratio \\
\bottomrule
\end{tabular}}
\end{center}

\subsection{Schritt 3: Self-consistency}

Die Fixed pointbedingung \eqref{eq:xi0-aus-fixpunkt} mit den Leptonen-Exponenten
$p_e = 3/2$, $p_\mu = 1$, $p_\tau = 2/3$ liefert eine Konsistenzprüfung.
Für das Elektron:
\begin{equation}
  \xi_0 = K_{\text{frak}}^{1/(3/2 - 1)}
  = K_{\text{frak}}^2
  \approx (0{,}986)^2 \approx 0{,}972 \times 10^{-4}
\end{equation}

Die vollständige Auflösung erfolgt über das e-p-$\mu$-System
(Doc.~180): der Exponent $\kappa = 7$ ist die einzige ganzzahlige
Rekursionstiefe, die alle drei Conditionen gleichzeitig erfüllt.

%------------------------------------------------------------
\section{Die Rekursionsformel}
%------------------------------------------------------------

Der Rekursionsoperator erzeugt die mass hierarchy durch wiederholte
Anwendung:
\begin{equation}
  \mathcal{R}^{(k)}(\mathbf{e}_{n,l,j})
  = \xi_0^{k(p_{n,l,j}-1)} \cdot K_{\text{frak}}^k
  \cdot \mathbf{e}_{n,l,j}
  \label{eq:rekursion-k}
\end{equation}

Für $k = \kappa = 7$ (Rekursionstiefe aus Doc.~180):
\begin{equation}
  \mathcal{R}^{(7)}(\mathbf{e}_e) = \xi_0^{7(3/2-1)} \cdot K_{\text{frak}}^7
  \cdot \mathbf{e}_e
  = \xi_0^{7/2} \cdot K_{\text{frak}}^7 \cdot \mathbf{e}_e
\end{equation}

Die Self-consistencybedingung verlangt, dass dieser Ausdruck mit der
bekannten Elektronenmasse consistent ist. Das fixiert $\xi_0$
auf den eindeutigen Value $4/30000$.

%------------------------------------------------------------
\section{Summary: R als formaler Rahmen}
%------------------------------------------------------------

\begin{center}
{\footnotesize
\begin{tabular}{p{4.5cm}p{6cm}}
\toprule
\textbf{Physikalische Derivation} (Doc.~180) &
\textbf{Operator-Sprache} (Doc.~203) \\
\midrule
$10^{-4}$ aus 4D-Spacetime &
  $\mathcal{R}^{(4D)}$ skaliert mit $\xi_0^4$ \\[4pt]
$4/3$ aus Tetraeder-Geometry &
  Geometryfaktor in $K_{\text{frak}}$ \\[4pt]
$\kappa = 7$ aus e-p-$\mu$-System &
  $\mathcal{R}^{(7)}(\Phi_*) = \Phi_*$ Fixed pointbedingung \\[4pt]
$\xi_0 = 4/30000$ eindeutig &
  Einzige Lösung der Self-consistency \\
\bottomrule
\end{tabular}
}
\end{center}

Der Rekursionsoperator $\mathcal{R}$ ist damit kein neues Postulat,
sondern die formale Darstellung der in Doc.~180 abgeleiteten
Geometry. Die Task aus Doc.~202, Problem~1 ist damit gelöst.

\medskip\noindent
%------------------------------------------------------------
\section{Note: Apparent Contradiction from Category Error}
%------------------------------------------------------------

The fixed-point condition $\mathcal{R}(\Phi_*) = \Phi_*$ might suggest
requiring $\lambda_i = 1$ for each mode $i$. Substituting
\eqref{eq:eigenwerte} gives:
\[
  K_{\text{frak}}(e) = \xi_0^{-1/2} \approx 86.6, \quad
  K_{\text{frak}}(\mu) = 1, \quad
  K_{\text{frak}}(\tau) = \xi_0^{1/3} \approx 0.051
\]

These values differ --- an apparent contradiction to a universal
$K_{\text{frak}} \approx 0.986$.

\textbf{However:} This is \textbf{the same category error}
as in the non-closure theorem (Doc.~193). Comparing expressions
from different mode spaces --- just as $r_e \cdot r_\mu$ is
meaningless because $r_e$ and $r_\mu$ label different algebraic
spaces, the comparison $K_{\text{frak}}(e) = K_{\text{frak}}(\mu)$
is inadmissible.

The fixed-point condition is a \textbf{global} condition on the field
operator as a whole, not a per-mode requirement $\lambda_i = 1$.
$K_{\text{frak}} \approx 0.986$ is a single geometric constant
of the fractal torus (Doc.~180) --- fully derived, no further
derivation needed.


%------------------------------------------------------------
\section*{Notation}
%------------------------------------------------------------

\begin{center}
{\footnotesize
\resizebox{\textwidth}{!}{\begin{tabular}{lp{9cm}}
\toprule
\textbf{Symbol} & \textbf{Meaning} \\
\midrule
$\mathcal{R}$ & Recursion operator in mode space \\
$\lambda_{n,l,j}$ & Eigenvalue of $\mathcal{R}$ for mode $(n,l,j)$ \\
$\lambda_{n,l,j} = \xi_0^{p-1}\cdot K_{\text{frak}}$ & Explicit eigenvalue formula \\
$K_{\text{frak}} \approx 0.986$ & Fractal correction constant (universal, Doc.~180) \\
$\kappa = 7$ & Recursion depth (from e-p-$\mu$ self-consistency) \\
$\Phi_* = \sum_i m_i P_i$ & Fixed-point state of the operator \\
$\mathcal{R}(\Phi_*) = \Phi_*$ & Fixed-point condition \\
$p(n,l,j)$ & Exponent function from torus geometry \\
$\Delta p = 1/3$ & Step size of lepton exponents \\
$\xi_0 = 4/30000$ & Geometric base constant (Doc.~180) \\
$P_i = |\mathbf{e}_i\rangle\langle\mathbf{e}_i|$ & Projector onto mode $i$ (orthogonal) \\
$m_i = r_i\cdot\xi_0^{p_i}\cdot v$ & FFGFT mass formula \\
\bottomrule
\end{tabular}}}
\end{center}

\medskip\noindent
\textbf{References:} Doc.~006 ($r_i$, $p_i$), Doc.~051 (spinor),
Doc.~180 ($\xi_0$), Doc.~189 (stage structure),
Doc.~193 (non-closure theorem), Doc.~202 (overview).
