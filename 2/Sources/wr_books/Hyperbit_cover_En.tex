\documentclass[11pt]{article}
\usepackage[
  paperwidth=6in,
  paperheight=9in,
  margin=0pt
]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage[math-style=ISO]{unicode-math}
\setmainfont{Inter}[
  UprightFont=*-Regular,
  BoldFont=*-Bold,
  ItalicFont=*-Italic
]
\setmathfont{Libertinus Math}
\usetikzlibrary{calc,decorations.pathreplacing,fadings,shadings}
\pagestyle{empty}

\definecolor{deepblue}{RGB}{10,30,80}
\definecolor{midblue}{RGB}{30,80,160}
\definecolor{lightblue}{RGB}{80,140,220}
\definecolor{gold}{RGB}{200,160,40}
\definecolor{bgdark}{RGB}{8,18,50}

\begin{document}
\begin{tikzpicture}[remember picture, overlay]

  % Hintergrund: tiefdunkles Nachtblau
  \fill[bgdark] (current page.south west) rectangle (current page.north east);

  % Torus-Andeutung: konzentrische Ellipsen im Hintergrund (geometrisches Motiv)
  \foreach \r in {0.6,1.0,1.4,1.8,2.2,2.6,3.0,3.4}{
    \draw[lightblue!15, line width=0.3pt]
      ($(current page.center)+(0,0.8cm)$)
      ellipse [x radius={\r*2.2cm}, y radius={\r*0.7cm}];
  }
  % Torus-Windungen (vertikale Ellipsen)
  \foreach \a in {0,40,...,320}{
    \draw[lightblue!10, line width=0.2pt]
      ($(current page.center)+(0,0.8cm)$)
      ellipse [x radius={0.9cm}, y radius={2.0cm},
               rotate=\a];
  }

  % Farbiger Akzentstreifen oben
  \fill[midblue!80]
    (current page.north west)
    rectangle
    ($(current page.north east)+(0,-1.2cm)$);

  % Goldene Linie als Trennlinie
  \draw[gold, line width=1.5pt]
    ($(current page.north west)+(0,-1.2cm)$)
    -- ($(current page.north east)+(0,-1.2cm)$);

  % Haupttitel
  \node[anchor=north, text width=13cm, align=center]
    at ($(current page.north)+(0,-1.6cm)$)
    {{\fontsize{34}{40}\bfseries\color{white}
      Bits, Hyperbits\\[4pt]
      and Landauer}};

  % Untertitel
  \node[anchor=north, text width=13cm, align=center]
    at ($(current page.north)+(0,-4.2cm)$)
    {{\fontsize{14}{18}\itshape\color{lightblue!90}
      Information Physics within the FFGFT Framework}};

  % Goldene Trennlinie Mitte
  \draw[gold!60, line width=0.8pt]
    ($(current page.center)+(-5cm,2.0cm)$)
    -- ($(current page.center)+(5cm,2.0cm)$);

  % Zentrales Motiv: die Kernformel
  \node[anchor=center]
    at ($(current page.center)+(0,0.9cm)$)
    {{\fontsize{22}{28}\color{gold}
      $E_{\mathrm{bit}}(L) = \dfrac{\hbar c}{L}$}};

  \node[anchor=north, text width=11cm, align=center]
    at ($(current page.center)+(0,-0.2cm)$)
    {{\fontsize{10}{13}\color{lightblue!70}
      \textit{versus}\quad$k_B T \ln 2$}};

  % Beschreibungstext
  \node[anchor=north, text width=12cm, align=center]
    at ($(current page.center)+(0,-1.4cm)$)
    {{\fontsize{9.5}{13}\color{white!80}
      Landauer $\cdot$ Matzke $\cdot$ p-Bit $\cdot$ Torus $T^4$\\[3pt]
      Galois Algebra $\cdot$ Winding Quanta $\cdot$ Scale Anchor}};

  % Goldene Trennlinie unten
  \draw[gold!60, line width=0.8pt]
    ($(current page.center)+(-5cm,-2.6cm)$)
    -- ($(current page.center)+(5cm,-2.6cm)$);

  % Autor
  \node[anchor=north, text width=12cm, align=center]
    at ($(current page.center)+(0,-2.9cm)$)
    {{\fontsize{13}{16}\bfseries\color{white}
      Johann Pascher}};

  \node[anchor=north, text width=12cm, align=center]
    at ($(current page.center)+(0,-3.6cm)$)
    {{\fontsize{8}{10}\color{lightblue!60}
      ORCID 0009-0000-6518-4064}};

  % Unterer Balken
  \fill[midblue!60]
    (current page.south west)
    rectangle
    ($(current page.south east)+(0,1.5cm)$);

  \draw[gold, line width=1.2pt]
    ($(current page.south west)+(0,1.5cm)$)
    -- ($(current page.south east)+(0,1.5cm)$);

  % Verlagsinfo unten
  \node[anchor=south, text width=13cm, align=center]
    at ($(current page.south)+(0,0.3cm)$)
    {{\fontsize{8}{10}\color{white!70}
      T0 Time-Mass Duality $\cdot$
      Fundamentale Fraktale Geometrische Feldtheorie $\cdot$
      September 2026}};

\end{tikzpicture}
\end{document}
