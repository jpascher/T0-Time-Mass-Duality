% 343_Zeta_Galois_FFGFT_En_ch.tex
% Johann Pascher, 2 September 2026
% Continuation of Doc. 342 — consequences of Galois factorisation for the
% zeta function of the T⁴/Z₃ torus

\providecommand{\BM}{\textbf{[B]}}
\providecommand{\KM}{\textbf{[K]}}
\providecommand{\SM}{\textbf{[S]}}

\chapter*{The Zeta Function of the T$^4$/Z$_3$ Torus}
\addcontentsline{toc}{chapter}{The Zeta Function of the T$^4$/Z$_3$ Torus}

\begin{abstract}
The Galois factorisation classes of Doc.~342 and the harmonic series on the
T$^4$ torus of Doc.~159 together yield a precise statement about the zeta
function of the FFGFT spacetime.

Four results:
\textbf{(A)}~The Z$_3$ orbifold projection of T$^4$/Z$_3$ transforms the full
Riemann zeta into a Dirichlet L-function:
$\zeta_{T^4/\mathbb{Z}_3}(s) = L(s,\chi_0) = (1-3^{-s})\,\zeta(s)$ \BM.
\textbf{(B)}~The Euler product of this L-function is hierarchically ordered by
the entry depth $k = \mathrm{ord}_p(3)$; the physically visible primes
$\{2,3,5,7,11,13\}$ are exactly those with $k \leq 6$ \BM.
\textbf{(C)}~The orbifold projection generates additional zeros at
$s = 2\pi i k/\ln 3$ ($k \in \mathbb{Z} \setminus \{0\}$) on the imaginary
axis $\mathrm{Re}(s) = 0$; these are the algebraic signature of the Z$_3$
sector separation \BM.
\textbf{(D)}~The four Frobenius conjugacy classes of primitive elements in
GF$(27)^*$ (Doc.~342, Theorem~A) are paired into \emph{two} classes by the
FFGFT sector pairing $k \leftrightarrow -k$ (Doc.~336; on GF$(27)^*$: $x \mapsto x^{-1}$),
$f_1 \leftrightarrow f_2$ and $f_3 \leftrightarrow f_4$; negation
$x \mapsto -x = x^{13}\,x$ couples each primitive class to exactly one order-13
class \BM. The Galois character of $\mathrm{Gal}(\mathrm{GF}(27)/\mathrm{GF}(3)) \cong \mathbb{Z}_3$
does not distinguish the classes; the distinction lies in
$(\mathbb{Z}/26)^*/\langle 3\rangle \cong \mathbb{Z}_4$. Sector pairing and the Majorana criterion $k\mapsto k^{-1}$ (Doc.~340) partition
the two layers identically: $\{f_3,f_4\}$ Dirac (charged leptons), $\{f_1,f_2\}$
Majorana ($\nu_1,\nu_2$ active; $\nu_3$ in Dirac orbit $O_4$) \BM;
prediction: $\nu_3$ has no Majorana phase \BM.

\subsection{Theorem D$'''$: quantum numbers of the second layer}

What quantum numbers would the two layers carry? The algebra answers
this without physical assumption (\texttt{pruef\_343f\_zweite\_schicht.py}).
Each primitive class $f_i$ lies in exactly one $\mathbb{Z}_{13}$ orbit of the
Frobenius (Doc.~339); with the exponents of Table~3: $f_1=\{1,3,9\} \mapsto O_1$,
$f_2=\{17,23,25\} \mapsto O_3=\{4,10,12\}$, $f_3=\{5,15,19\} \mapsto O_2=\{2,5,6\}$,
$f_4=\{7,11,21\} \mapsto O_4=\{7,8,11\}$. Doc.~340 takes the
self-inverseness of an orbit under $k \mapsto k^{-1} \pmod{13}$ as the Majorana
criterion. Both involutions together give \BM:

\begin{table}[H]
\centering
\caption{Quantum numbers of the four primitive classes from the algebra}
\begin{tabular}{llllll}
\toprule
Class & $\mathbb{Z}_{13}$ orbit & $k\mapsto -k$ & $k\mapsto k^{-1}$ & profile & layer \\
\midrule
$f_1$ & $O_1$ & $\to f_2$ & self-inverse & Majorana & $\{f_1,f_2\}$ \\
$f_2$ & $O_3$ & $\to f_1$ & self-inverse & Majorana & $\{f_1,f_2\}$ \\
$f_3$ & $O_2$ & $\to f_4$ & $\to O_4$ & Dirac & $\{f_3,f_4\}$ \\
$f_4$ & $O_4$ & $\to f_3$ & $\to O_2$ & Dirac & $\{f_3,f_4\}$ \\
\bottomrule
\end{tabular}
\end{table}

The sector pairing $k \mapsto -k$ and the Majorana criterion $k \mapsto k^{-1}$
partition the four classes identically: \textbf{$\{f_1,f_2\}$ is the Majorana
layer, $\{f_3,f_4\}$ the Dirac layer} \BM. \textit{Clarification:} The Majorana
criterion is an algebraic property of the Galois orbit (self-inverseness under
$k\mapsto k^{-1}$ mod~13). Whether this implies the physical Majorana condition
($\nu=\bar{\nu}$) is an additional assumption \SM; see Doc.~344 for the open
anti-neutrino question. With the negation $f_i \leftrightarrow g_i$
the numbering of Doc.~342 is thus the natural one: sector pairing pairs adjacent
indices, negation equal indices. Both layers carry $\mathbb{Z}_2$
parity~1 (massive sector, Doc.~339), no colour, and three states per class
(three-cycle).

\textbf{Interpretation \BM/\SM.} Doc.~341 fixes order~26 in G$(6)$ for the
leptons; the class follows here: charged leptons are Dirac particles and belong
to $\{f_3,f_4\}$ \BM. The algebraic Majorana layer $\{f_1,f_2\}$ is fully
occupied by active neutrinos: Doc.~340 places $\nu_1$ in $O_1=f_1$
(orbit-algebraically Majorana) and $\nu_2$ in $O_3=f_2$ (orbit-algebraically
Majorana). Doc.~340 also places $\nu_3$ in $O_4$ --- a Dirac orbit (not
self-inverse). This is not a contradiction: the quadratic character mod~13
determines the property of the field. $\nu_3$ in $O_4$ is therefore a
\textbf{Dirac neutrino} \BM. There is no empty slot and no prediction of
sterile neutrinos from this algebra.

\textit{Clarification on anti-neutrinos \SM.} The Majorana criterion
is an algebraic property of the Galois orbit (self-inverseness under
$k\mapsto k^{-1}$ mod~13) \BM. Whether this implies the physical Majorana
condition ($\nu=\bar{\nu}$) is an additional assumption. In the Furey/GALG
construction (Doc.~344) the anti-neutrino Sd[0] $=$ Vss[1] $\neq$
Vss[0] $=$ Su[0], so neutrinos there are Dirac particles \SM.

\textbf{Testable prediction \BM:} $\nu_3$ has no Majorana phase;
$m_{ee}$ depends only on $\nu_1$ and $\nu_2$:
\[
  m_{ee} = |m_1 U_{e1}^2 + m_2 U_{e2}^2|
  \leq m_{\nu_1} + m_{\nu_2} \approx 14.4\,\text{meV}.
\]
Doc.~340 computes $m_{ee}\approx7.1$~meV. The inverted hierarchy is more
strongly excluded because the $\nu_3$ contribution to $m_{ee}$ vanishes.
Testable at KamLAND-Zen and nEXO (sensitivity $\sim10$~meV).

\textbf{Connection to Doc.~006 \SM.} Doc.~006 had already stated the same
qualitative finding: neutrinos experience an additional $\xi$ suppression
factor relative to charged leptons (``double $\xi$ suppression'').
Theorem~D$'''$ provides the algebraic reason: Majorana orbits ($O_1$, $O_3$)
carry this double factor, the Dirac orbit ($O_4$, $\nu_3$) does not.
Quantitatively, however, the two documents are inconsistent: Doc.~006 gives
$m_{\nu_e}=9.1$, $m_{\nu_\mu}=1.9$, $m_{\nu_\tau}=18.0$~meV --- compared to
Doc.~340's $m_{\nu_1}=4.54$, $m_{\nu_2}=9.81$, $m_{\nu_3}=49.9$~meV.
Discrepancies up to a factor of~9; the ordering differs. Doc.~340
is the more recent and algebraically better founded result.


Two further results:
\textbf{(E)}~The harmonic primes $\{5,7,11,13\}$ are uniquely forced by the
prime factorisations of $3^k-1$ for $k=3,4,5,6$; $p=13$ is the worldwide
unique prime with $\mathrm{ord}_p(3)=3$ \BM.
\textbf{(F)}~The Euler factor threshold at $p^*\approx9.76$ ($s=2$) marks
the 7-limit exactly; from $p_\mathrm{max}\approx30$ the Galois product density
lies at chance level; the Weyl obstruction (Doc.~316) shows that the limit
lies in the object; Shor's circuit model is digital and unaffected, but the physical apparatus is a
resonant system subject to the resonance limits as long as the threshold theorem
is not demonstrated at scale \SM; empirically no quantum advantage has been
demonstrated [X].

Check scripts: \texttt{pruef\_343\_zeta\_galois.py},
\texttt{pruef\_343b\_klassen\_symmetrie.py},
\texttt{pruef\_344\_primzahlmuster.py}.
\end{abstract}

% ============================================================
\section{Status markers and starting point}
% ============================================================

The following status markers are used throughout:
\begin{center}
\begin{tabular}{@{}lp{10.5cm}@{}}
\textbf{[K]} & \emph{Confirmed} — fully proved from $\xi$ or a corpus-established
  derivation; all check-script assertions passed. \\[4pt]
\textbf{[B]} & \emph{Established} — numerically secured; analytical proof present or trivial. \\[4pt]
\textbf{[S]} & \emph{Speculative} — indication or conjecture, not yet fully proved. \\
\end{tabular}
\end{center}

Doc.~159 shows that the spectral zeta function of the Laplace operator on a torus
$T^d$ is the Epstein zeta function, whose one-dimensional special case is the
Riemann zeta $\zeta(s) = \sum_{n=1}^\infty n^{-s}$.
Doc.~342 shows that primes appear in the GF$(3^k)$ hierarchy of the T$^4$/Z$_3$
orbifold ordered by entry depth $k = \mathrm{ord}_p(3)$.
This document combines both: what does the GF$(3^k)$ hierarchy imply for the
zeta function itself?

% ============================================================
\section{Theorem A: Orbifold projection and Dirichlet L-function}
% ============================================================

\subsection{Decomposition of the zeta by Z$_3$ classes}

On T$^4$/Z$_3$ the allowed modes $n \in \mathbb{Z}^4$ are structured by
Z$_3$ invariance. In the one-dimensional analogue $\zeta(s)$ splits into
three partial sums by $n \bmod 3$ \BM:
\begin{equation}
  \zeta(s) = \underbrace{\sum_{\substack{n=1\\3\mid n}}^\infty \frac{1}{n^s}}_{= 3^{-s}\zeta(s)}
  + \underbrace{\sum_{\substack{n=1\\n\equiv 1(3)}}^\infty \frac{1}{n^s}}_{L_1(s)}
  + \underbrace{\sum_{\substack{n=1\\n\equiv 2(3)}}^\infty \frac{1}{n^s}}_{L_2(s)}
\end{equation}
The fixed-point sector ($n \equiv 0$) contributes exactly $3^{-s}\zeta(s)$;
the twist sectors ($n \equiv \pm 1$) together $(1 - 3^{-s})\zeta(s)$.

\subsection{Principal character $\chi_0$ mod~3}

The Dirichlet character $\chi_0 \bmod 3$ with $\chi_0(n) = 0$ if $3 \mid n$,
otherwise $\chi_0(n) = 1$, gives \BM:
\begin{equation}
  L(s,\chi_0) = \sum_{\gcd(n,3)=1} \frac{1}{n^s}
  = (1 - 3^{-s})\,\zeta(s)
  = \prod_{p \neq 3} \frac{1}{1-p^{-s}}
\end{equation}
This is the \textbf{spectral zeta function of the T$^4$/Z$_3$ torus}: the
characteristic prime $p=3$ does not appear as an Euler factor — it is the
geometric foundation of the orbifold and stands outside the prime hierarchy.

\subsection{Non-trivial character $\chi_1$ mod~3}

The character $\chi_1 \bmod 3$ with $\chi_1(1) = +1$, $\chi_1(2) = -1$,
$\chi_1(0) = 0$ measures the \emph{asymmetry} of the twist sectors \BM:
\begin{equation}
  L(s,\chi_1) = \sum_{\substack{n=1\\n\equiv 1(3)}}^\infty \frac{1}{n^s}
              - \sum_{\substack{n=1\\n\equiv 2(3)}}^\infty \frac{1}{n^s}
\end{equation}
with the exact value (Dirichlet 1837): $L(1,\chi_1) = \pi/(3\sqrt{3})$.

Physical meaning \SM: $L(s,\chi_1)$ measures the difference between winding
numbers $w = +1$ and $w = -1$ on the torus, i.e.\ the Z$_3$ sector asymmetry.
Whether $L(s,\chi_1)$ has a direct physical observable in FFGFT
(particle–antiparticle asymmetry in the twisted sector?) remains open \SM.

% ============================================================
\section{Theorem B: Hierarchical Euler product by $k = \mathrm{ord}_p(3)$}
% ============================================================

The primes in $L(s,\chi_0) = \prod_{p\neq 3}(1-p^{-s})^{-1}$ are not
equivalent. By the entry-depth rule $k = \mathrm{ord}_p(3)$ from
Doc.~342, Theorem~B, the Euler product is hierarchically ordered \BM:
\begin{equation}
  L(s,\chi_0) = \prod_{k=1}^{\infty} \prod_{\substack{p\neq 3\\ \mathrm{ord}_p(3)=k}}
  \frac{1}{1 - p^{-s}}
\end{equation}

\begin{table}[H]
\centering
\caption{Hierarchy of Euler factors by $k = \mathrm{ord}_p(3)$}
\begin{tabular}{rrlll}
\toprule
$k$ & new prime(s) & Euler factor at $s=2$ & harmonics & FFGFT role \\
\midrule
1 & 2 & $4/3 = 1.333$ & octave & Z$_2$ fixed point, massive sector \\
3 & 13 & $\approx 1.006$ & thirteenth & GF$(27)^*$, leptons \\
4 & 5 & $\approx 1.042$ & major third & factor in $(m_\mu/m_e)^2$ \\
5 & 11 & $\approx 1.008$ & eleventh & neutrinos (Doc.~340) \\
6 & 7 & $\approx 1.021$ & seventh & top quark (Doc.~289) \\
$\geq 7$ & $\geq 41, 1093,\ldots$ & $\approx 1+p^{-2}$ & no small harmonics & suppressed \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Fractal suppression of higher Euler factors}

On the fractal T$^4$ torus (Doc.~159) high-mode contributions are suppressed
by the damping factor $\tau^{-(1+\xi)k}$. This transfers to the Euler factors;
the \emph{weighted} Euler product reads
\begin{equation}
  \zeta_{T^4/\mathbb{Z}_3}^{\,\mathrm{frac}}(s)
  \;\cong\; \prod_{k=1}^{\infty}\;
  \prod_{\substack{p\neq 3\\ \mathrm{ord}_p(3)=k}}
  \left(\frac{1}{1-p^{-s}}\right)^{\!\tau^{-k(1+\xi)}}
\end{equation}
Since $\tau^{-k(1+\xi)} \to 0$ as $k\to\infty$, each Euler factor is pushed
toward~1 for large~$k$: primes with $\mathrm{ord}_p(3) \geq 7$ make no physical
contribution \BM. This is Theorem~D of Doc.~342 in the language of the zeta function.

% ============================================================
\section{Theorem C: New zeros from the orbifold projection}
% ============================================================

\subsection{Zeros of $(1-3^{-s})$}

The factor $(1-3^{-s})$ in $L(s,\chi_0) = (1-3^{-s})\zeta(s)$
has zeros at \BM:
\begin{equation}
  3^{-s} = 1 \quad\Longleftrightarrow\quad s = \frac{2\pi i\,k}{\ln 3},
  \qquad k \in \mathbb{Z} \setminus \{0\}
\end{equation}
These lie on $\mathrm{Re}(s) = 0$ (imaginary axis), \emph{not} on the
critical line $\mathrm{Re}(s) = 1/2$ of the Riemann hypothesis.
They are absent from the Riemann zeta spectrum; they arise solely from the
Z$_3$ orbifold projection.

The zero spacing $2\pi/\ln 3 \approx 5.72$ encodes the periodicity of the
Z$_3$ lattice structure. A resonance test of the first eight known Riemann
zeros $\gamma_n$ against this unit shows no significant coincidence
(deviations $0.15$--$0.47$) \BM: the full Riemann zeta encodes the distribution
of \emph{all} primes, not only the Z$_3$-compatible ones.

\subsection{Complete zero structure of the orbifold zeta}

$L(s,\chi_0)$ has three types of zeros:
\begin{itemize}
  \item \textbf{Trivial zeros of $\zeta(s)$}: at $s = -2,-4,-6,\ldots$
  \item \textbf{Non-trivial Riemann zeros}: on $\mathrm{Re}(s)=1/2$ (assumed), unchanged
  \item \textbf{Orbifold zeros}: at $s = 2\pi ik/\ln 3$, $k\neq 0$
        — new, generated by the Z$_3$ projection \BM
\end{itemize}

% ============================================================
\section{Theorem D: The four classes under sector pairing}
% ============================================================

\subsection{The Galois character does not distinguish the classes}

The Galois group $\mathrm{Gal}(\mathrm{GF}(27)/\mathrm{GF}(3)) = \langle F\rangle \cong \mathbb{Z}_3$
with $F\colon x \mapsto x^3$ has three characters $\chi_j(F) = \omega^j$, $j=0,1,2$.
All four primitive classes $f_1,\ldots,f_4$ of Doc.~342 are \emph{regular} $F$-orbits
of length~3: $F$ acts on each as a 3-cycle, and the associated character is the same
for all four. The Galois character therefore does not separate the classes \BM.

The distinction lies in the discrete logarithm: with a generator $x$ of GF$(27)^*$,
each class is an orbit $\{x^k, x^{3k}, x^{9k}\}$ with $\gcd(k,26)=1$, i.e.\ a coset
of $\langle 3\rangle = \{1,3,9\}$ in $(\mathbb{Z}/26)^* \cong \mathbb{Z}_{12}$.
There are $12/3 = 4$ cosets \BM:

\begin{table}[H]
\centering
\caption{The four primitive classes as cosets of $\langle 3\rangle$ in $(\mathbb{Z}/26)^*$
  (basis $x^3 + 2x + 1 = 0$)}
\begin{tabular}{llll}
\toprule
Class & Minimal polynomial & Exponents $k$ & Role \\
\midrule
$f_1$ & $x^3 + 2x + 1$ & $\{1, 3, 9\}$ & Majorana layer (Theorem~D$'''$) \\
$f_2$ & $x^3 + 2x^2 + 1$ & $\{17, 23, 25\}$ & $= f_1^{-1}$, Majorana layer \\
$f_3$ & $x^3 + 2x^2 + x + 1$ & $\{5, 15, 19\}$ & Dirac layer \\
$f_4$ & $x^3 + x^2 + 2x + 1$ & $\{7, 11, 21\}$ & $= f_3^{-1}$, Dirac layer \\
\bottomrule
\end{tabular}
\end{table}

The quotient $(\mathbb{Z}/26)^*/\langle 3\rangle \cong \mathbb{Z}_4$ is cyclic;
inversion $k \mapsto -k$ is its element of order~2.

\subsection{Sector pairing reduces four to two}

Doc.~336 identifies the FFGFT sector pairing $k \leftrightarrow -k$ with the
Frobenius automorphism; at the level of elements of GF$(27)^*$ it is inversion
$x \mapsto x^{-1} = x^{25}$. It permutes the classes as \BM
\begin{equation}
  f_1 \longleftrightarrow f_2, \qquad f_3 \longleftrightarrow f_4 .
\end{equation}
If sector pairing is a symmetry of the mass layer --- as it is in the corpus ---
there are not four but \textbf{two} physically distinguishable GF$(27)^*$ layers:
$\{f_1, f_2\}$ and $\{f_3, f_4\}$ (assignment in Theorem~D$'''$).

\subsection{Negation couples order 26 to order 13}

Since $-1 = x^{13}$ in GF$(27)$, the map $x \mapsto -x$ sends every element of
order~26 to one of order~13 and vice versa. The four order-13 classes
$g_1,\ldots,g_4$ of Doc.~342 are therefore not an independent structure but the
sign partners of the primitive classes \BM:
\begin{equation}
  f_i \leftrightarrow g_i \qquad (i = 1, \ldots, 4).
\end{equation}
The candidate ``sub-lepton structure'' from the order-13 polynomials, left open in
Doc.~342, is thereby eliminated.

\subsection{Open question}

The reduction $4 \to 2$ sharpens the question of Doc.~342: not ``do $f_2, f_3, f_4$
carry further layers'', but ``there are exactly two GF$(27)^*$ layers
$\{f_1, f_2\}$ and $\{f_3, f_4\}$ --- which particles carry which?''
Theorem~D$'''$ answers this from the algebra.

% ============================================================
\section{Theorem E: Prime patterns in the GF$(3^k)$ hierarchy}
% ============================================================

\subsection{The harmonic primes are uniquely forced}

A prime $p$ satisfies $\mathrm{ord}_p(3) = k$ if and only if $p$ divides
$3^k - 1$ and $p \nmid 3^j - 1$ for all $j < k$.
The prime factorisations of the first relevant values give \BM:

\begin{table}[H]
\centering
\caption{Prime factors of $3^k - 1$ and the forced physical primes}
\begin{tabular}{rrlll}
\toprule
$k$ & $3^k - 1$ & prime factors & phys.\ prime & note \\
\midrule
1 & 2 & $\{2\}$ & 2 & octave \\
3 & 26 & $\{2, 13\}$ & \textbf{13} & unique prime with $\mathrm{ord}_p(3)=3$ worldwide \\
4 & 80 & $\{2, 5\}$ & \textbf{5} & only non-2 factor \\
5 & 242 & $\{2, 11\}$ & \textbf{11} & only non-2 factor \\
6 & 728 & $\{2, 7, 13\}$ & \textbf{7} & (13 has $\mathrm{ord}_{13}(3)=3$, not 6) \\
\bottomrule
\end{tabular}
\end{table}

The strongest result concerns $p = 13$ \BM: since $3^3 - 1 = 26 = 2 \cdot 13$
and $\mathrm{ord}_2(3) = 1$, $p = 13$ is the \emph{unique} prime worldwide with
$\mathrm{ord}_p(3) = 3$. There is no other, and there can be none. The physical
primes $\{5, 7, 11, 13\}$ are not selected by harmonic considerations, but are
completely and uniquely forced by the prime-factor structure of $3^k - 1$
for $k = 3, 4, 5, 6$.

\subsection{Necessary congruence condition}

From $\mathrm{ord}_p(3) = k$ with $3 \mid k$ it follows that $3 \mid (p-1)$,
i.e.\ $p \equiv 1 \pmod{3}$ \BM. In particular $p = 13$ (at $k=3$) and
$p = 7$ (at $k=6$) are both $\equiv 1 \pmod{3}$ — exactly the
Z$_3$-compatible primes of the corpus (Doc.~342, Theorem~B).

% ============================================================
\section{Theorem F: Limits of distinguishability and computational cost}
% ============================================================

\subsection{Exact threshold: Euler factor vs.\ fractal correction}

The Euler factor $1/(1-p^{-s})$ exceeds the fractal correction $1.05\,\%$
(Doc.~338, bare to measured) exactly for $p < p^*$ with
$p^* = (0.0105)^{-1/s}$ \BM:

\begin{table}[H]
\centering
\caption{Exact threshold $p^*$ by value of $s$}
\begin{tabular}{rrlll}
\toprule
$s$ & $p^*$ & primes $\leq p^*$ & harmonic limit & Euler factor $\geq 1.05\,\%$ \\
\midrule
1 & 95.2 & $\{2,3,5,\ldots,89\}$ & --- & all small primes \\
2 & 9.76 & $\{2, 3, 5, 7\}$ & 7-limit & octave, fifth, third, seventh \\
3 & 4.57 & $\{2, 3\}$ & 3-limit & octave and fifth only \\
\bottomrule
\end{tabular}
\end{table}

For $s = 2$ (reference value of the spectral zeta) the threshold is
$p^* \approx 9.76$: primes $\{2, 5, 7\}$ have Euler factors exceeding the
fractal correction; $p = 11$ and all larger primes fall below it. This is
exactly the harmonic 7-limit. $p = 11$ appears in the corpus (neutrinos,
Doc.~340) via the structural formula $\Delta m^2 = (11^2-1)\,m_\nu^2$,
not as a Galois product hit.

\subsection{Galois products vs.\ random products: hit rate}

Forming all products of up to three Galois building blocks with exponents
$\{-1, 1, 2\}$ for $p \leq p_\mathrm{max}$, the product set grows rapidly
and the hit rate for arbitrary targets reaches chance level \BM:

\begin{table}[H]
\centering
\caption{Hit rate for physical vs.\ random targets at $\pm 0.5\,\%$}
\begin{tabular}{rrrrr}
\toprule
$p_\mathrm{max}$ & values & $43200$ & $m_\mu/m_e$ & random avg \\
\midrule
13  & 1334  & 2 & 0 & 0.92 \\
30  & 5802  & 3 & 2 & 3.60 \\
50  & 8946  & 3 & 2 & 5.33 \\
100 & 13398 & 4 & 3 & 7.66 \\
300 & 24992 & 8 & 12 & 14.15 \\
\bottomrule
\end{tabular}
\end{table}

From $p_\mathrm{max} \approx 30$ the hit rate lies at chance level
($\pm 0.5\,\%$); at the strict threshold $\pm 0.05\,\%$ already from
$p_\mathrm{max} \approx 13$. The identity
$43200 = |\mathrm{GF}(9)^*|^2 \cdot 5^2 \cdot |\mathrm{GF}(27)|$
(Doc.~338) uses only building blocks with $p \leq 5$ and is derived
\emph{geometrically} from $\xi$, not found by product search.

\subsection{Structural computational limit: Weyl obstruction and quantum computers}

The distinguishability threshold at $p_\mathrm{max} \approx 13$ is a
\emph{physical} limit (fractal correction). Independently of this, the corpus
contains a sharper \emph{mathematical} limit that applies to every technology \BM:

\begin{keyresult}[Weyl obstruction, Doc.~316]
There is no compact Riemannian manifold — of any dimension, curvature,
topology, or orbifold structure — whose Laplace spectrum consists of the
Riemann zeros. Reason: $T\ln T$ is not a power $T^{d/2}$.
\end{keyresult}

From Doc.~147, an $n$-qubit register is a compact state space with eigenvalue
density $N(\lambda) \sim \lambda^{d/2}$; arithmetic counting densities grow
as $T \ln T$. A finite state space cannot fully carry such a spectrum,
\emph{independently of the register size} \BM.

Doc.~075 names the consequence for Shor's algorithm:

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
Method & Limit & Type \\
\midrule
Trial division & $O(\sqrt{n})$ & computational \\
Pollard $\rho$ & $O(n^{1/4})$ & computational \\
Shor's QFT & resource limit & Weyl obstruction \\
\bottomrule
\end{tabular}
\end{center}

The central statement of Doc.~316 holds technology-independently:
\begin{quote}
\emph{The limit lies not in the tool but in the object.}
Transformation (Fourier, QFT, optics) projects and generates no information.
Relaxation finds what is already in the potential.
Both presuppose that the sought structure is already present.
\end{quote}

Physical limit ($p^* \approx 9.76$, Theorem~F.1) and mathematical limit
(Weyl obstruction, Theorem~F.3) thus appear in the same picture: beyond a
certain point the prime structure is hidden in the noise, and no analogue tool
retrieves it; Shor's algorithm is digital as a model,
a resonator as an apparatus --- and thus, until proven otherwise, bound as well (Theorem~G$'$).

% ============================================================
\section{In plain terms: what the limit means}
% ============================================================

\textbf{Question.} So arbitrarily dense harmonics can be computed --- but
beyond a certain depth they can no longer be distinguished?

\textbf{Answer.} Yes, exactly that.

\emph{Computation can go arbitrarily far.} GF$(3^k)$ for $k = 7, 8, 12, 100$
--- the algebra does not stop. Every $k$ yields new primes, new polynomial
classes, new chords. At $k = 12$ all four harmonic primes $5, 7, 11, 13$ sit
in one field simultaneously; at $k = 7$ the Wieferich prime $1093$ enters.
These structures are exactly computable and mathematically all present.

\emph{But beyond a certain depth they are physically meaningless}, for two
reasons that coincide.

First, the grid becomes too fine. For $k \leq 3$ the reachable values lie
about $3\,\%$ apart --- if $43200$ hits a mass value to $1\,\%$, that is a
result. For $k \geq 5$ they lie less than $0.5\,\%$ apart. Then \emph{every}
number hits something, and a hit means nothing. The control test in
Theorem~F shows it: two hundred random numbers hit as often as the actual
mass ratios.

Second --- and this is the physical reason --- the fractal correction of about
$1\,\%$ (Doc.~338, bare to measured) is itself already a statement about
higher winding modes. Once the Galois grid is finer than this correction,
nature itself no longer distinguishes neighbouring Galois values. The
structure is not gone; it has dissolved into the noise of the higher modes.

The analogy with harmonics is exact: everyone hears the overtones $2{:}1$
and $3{:}2$. $17{:}16$ is a semitone, $33{:}32$ a quarter-tone. From roughly
the 250th overtone the spacing falls below 5~cents --- mathematically exactly
present, acoustically no longer distinguishable. The series becomes a continuum.

\emph{The consequence for FFGFT.} The exact Galois layer reaches to GF$(27)$,
i.e.\ $k = 3$: the lepton layer, $43200$, the four classes, $p = 13$ as the
unique prime with $\mathrm{ord}_p(3) = 3$. Above that ($k = 4$, GF$(81)$,
$p = 5$) is already the transition. From $k = 5$ on, anything found by search
is indistinguishable from chance --- and must receive no status unless it
comes directly from $\xi$.

This is not a deficiency of the theory but its consistency: Doc.~159 says mode
convergence, Doc.~316 says Weyl obstruction, Doc.~342 and this document say the
same thing algebraically. There is a limit, it lies at $k \approx 3$--$4$, and
it comes from the subject itself, not from our computational capacity.

% ============================================================
\section{In plain terms II: synthesis, analysis, factorisation --- and coupling}
% ============================================================

\textbf{Question.} What does the limit mean for synthesis and analysis --- and
for factorisation? And how does coupling enter: Doc.~328 has three coupling
regimes; do pure numbers show the same picture as physical processes?

\subsection{Synthesis is unlimited, analysis is limited}

\emph{Synthesis} --- adding overtones, multiplying primes, forming
GF$(3^k)$ chords from partitions --- goes arbitrarily far and arbitrarily
dense. Nothing prevents superposing the 250th overtone with the 251st or
multiplying two 300-digit primes. Synthesis does not know the limit of
Theorem~F.

\emph{Analysis} --- which overtones are in the sound, which primes in the
number, which Galois class carries the mass --- hits exactly this limit. It
is not a matter of computational cost but of resolution: to separate overtone
$n$ from $n{+}1$ one needs a time window of about $n$ periods; to find the
period $r$ of a number, a frequency resolution $\Delta f \approx f_0/n^2$
(Doc.~075, precision barrier). Beyond a certain density the spacing lies
below the noise, and the decomposition is no longer unique --- not because it
does not exist, but because many decompositions fit equally well. That is the
chance-level finding of Theorem~F in other words.

\subsection{Factorisation is analysis}

Factorising $N = p \cdot q$ means reading the two fundamentals out of a beat.
For small $p, q$ --- the 7-limit range, $k \leq 3$ --- this works; those are
exactly the primes visible in the FFGFT spectrum at all. RSA primes with 300
digits sit at $k = \mathrm{ord}_p(3)$ of the order of $p$ itself, i.e.\ at a
depth undercutting the grid spacing of Theorem~F by hundreds of orders of
magnitude. Doc.~316 puts it thus: Fourier, QFT and optics project, they
generate no information; the period must already be in the prepared states,
and this preparation (modular exponentiation, $95\,\%$ of the gate cost,
Doc.~147) is exactly the problem one wanted to solve.

Two things follow. First: FFGFT does not break RSA --- and that is not a
deficiency but a consistent result. The asymmetry synthesis-easy /
analysis-hard on which RSA lives is the same asymmetry that ends the Galois
layer at $k \approx 3$--$4$. Second, for instrument building: an instrument can
\emph{produce} overtones arbitrarily densely; ear and analysis \emph{hear}
them sharply only up to about the 7-limit. What lies above acts as timbre,
not as pitch --- as the higher winding modes in FFGFT act as a $1\,\%$
correction, not as a separate mass layer.

\textbf{Caveat \SM.} Transferring Theorem~F to $\mathbb{Z}/N\mathbb{Z}$ is an
analogy. Doc.~075 records that $\xi$ has no function in
$\mathbb{Z}/N\mathbb{Z}$ and the period is fixed purely arithmetically by
$\mathrm{lcm}(p-1, q-1)$. The analogy holds; the fractal correction as the
\emph{mechanism} of the factorisation limit does not.

\subsection{Coupling: the same threefold division, not the same law}

Doc.~328 classifies couplings relative to loss rates: subcritical
($\kappa < 1$, one maximum), critical ($\kappa = 1$, maximally flat),
supercritical ($\kappa > 1$, splitting). Locking onto a frequency ratio $p/q$
occurs in Arnold tongues of width $\Delta\Omega(p/q) \propto K^q$.

For pure numbers the tolerance $\varepsilon$ takes the role of the coupling
strength: a real number ``locks'' onto $p/q$ if $|x - p/q| < \varepsilon$.
Fractions with denominator $\leq Q$ cover the interval with measure
$\approx 2\varepsilon \cdot 3Q^2/\pi^2$; coverage~1 gives the critical
denominator height \BM
\begin{equation}
  Q_c = \frac{\pi}{\sqrt{6\,\varepsilon}} .
\end{equation}
For $\varepsilon = 1.05\,\%$ (fractal correction), $Q_c \approx 12.5$ --- the
same order as $p_\mathrm{max} \approx 13$ in Theorem~F. The Galois version
confirms it numerically (\texttt{pruef\_343c}): at $\varepsilon = 1.05\,\%$ the
Galois grid with $p_\mathrm{max} = 7$ is subcritical (coverage $0.52$), with
$p_\mathrm{max} = 13$ critical ($0.92$), from $p_\mathrm{max} \approx 30$
supercritical ($1.00$). \textbf{The limit $k \approx 3$--$4$ of Theorem~F is
the critical point $\kappa = 1$ in the sense of Doc.~328} \BM.

So the threefold division is the same in arithmetic and physics. \emph{The
law behind it is not} \BM: Arnold tongues fall exponentially in $q$
($\Delta\Omega \propto K^q$), Farey spacings only polynomially ($1/q^2$). At
$K = 0.3$ and $q = 5$ the tongue is already $10^{-5}$ narrower than the
arithmetic capture interval; at $q = 13$ by $10^{-15}$. Physical locking is
exponentially selective, arithmetic distinguishability polynomial. Physics
``hears'' fewer harmonics than arithmetic permits --- the edge is the same,
but physically sharper.

\subsection{FFGFT is dynamically deeply subcritical}

With $K_\mathrm{eff} = 2\pi\xi \approx 8.4 \times 10^{-4}$ (Doc.~328) the
widest non-trivial Arnold tongue is $\Delta\Omega(1/2) \approx 5.6 \times
10^{-8}$, the total measure of all tongues $\approx 8 \times 10^{-4}$. Both
lie five to two orders of magnitude below the fractal correction \BM. No mass
ratio is pulled onto a $p/q$ by resonant locking.

This yields a clarification that was so far only implicit in the corpus: the
exact rationals --- $43200$, $8^2 \cdot 5^2 \cdot 27$, the winding numbers
$(r_i, p_i)$ --- are \emph{topological} (integer windings on T$^4$), not
\emph{dynamical} (locking). Discreteness and coupling are two separate
mechanisms in FFGFT. This is consistent with Doc.~328, where $\varphi$ lies
outside all tongues: the coupling is too weak to hold ratios; what is held,
topology holds.

% ============================================================
\section{Theorem G$'$: $\xi$ as resolution floor --- the limit depends on the level}
% ============================================================

\textbf{Question.} Is it excluded that $\xi$ plays a role in resonance finding
or analysis?

\textbf{Answer.} Not absolutely. Three roles must be distinguished. As a
\emph{selection criterion} in a search function of the form
$\exp(-(\omega-\omega_0)^2/4\xi)$, $\xi$ is excluded: the centre $\omega_0$ does
the work, $\xi$ only sets the width \BM. As a \emph{dynamical coupling}, $\xi$
is excluded: $K_\mathrm{eff}=2\pi\xi$ gives Arnold tongues of $5.6\times10^{-8}$
(Theorem~G$''$) \BM. As a \emph{resolution floor}, however, $\xi$ is the natural
scale: Doc.~328 sets $g/\omega=\xi$ as the linewidth of the torus modes,
Doc.~162 the fractal damping per operation as $K_\mathrm{frak}=1-100\xi$.

\subsection{The 1.05\,\% is one fractal level}

The correction bare $\to$ measured of Doc.~338 ($1.05\,\%$) and the damping
level $100\xi=1.33\,\%$ of Doc.~162 stand in the ratio $0.79$ \BM. The
thresholds in Theorems~F and~G were computed with $\varepsilon=1.05\,\%$ ---
they are thus the thresholds of \emph{one} fractal level, not of the theory.
If this level is subtracted analytically (it is computable per Docs.~338/162),
the resolving power moves to $\xi$:

\begin{table}[H]
\centering
\caption{Thresholds of Theorems~F/G for the corpus resolution levels
  (\texttt{pruef\_343d\_xi\_aufloesung.py})}
\begin{tabular}{lrrrrl}
\toprule
Resolution $\varepsilon$ & value & $Q_c$ & $p^*$ & primes $\leq p^*$ & Galois resolved to \\
\midrule
$1.05\,\%$ (Doc.~338) & $1.05\times10^{-2}$ & 12.5 & 9.8 & 4 & $k=3$ \\
$100\xi$ (Doc.~162) & $1.33\times10^{-2}$ & 11.1 & 8.7 & 4 & $k=3$ \\
$\xi$ (Doc.~328) & $1.33\times10^{-4}$ & 111 & 87 & 23 & $k\geq 8$ \\
$1/Q_\mathrm{TFLN}$ (Doc.~186) & $10^{-6}$ & 1283 & 1000 & 168 & $k\geq 8$ \\
$\xi^2$ & $1.78\times10^{-8}$ & 9619 & 7500 & 950 & $k\geq 8$ \\
\bottomrule
\end{tabular}
\end{table}

At $k_\mathrm{max}=8$ the Galois grid still has a mean spacing of $0.128\,\%$
--- ten times coarser than $\xi=0.0133\,\%$. At $\xi$ resolution the layer
remains resolved at least to $k=8$, and primes up to $83$ would have
distinguishable Euler factors \BM.

\subsection{Photonics as access to the $\xi$ floor}

Doc.~186 gives $Q>10^6$ for TFLN resonators. Resolving $\xi$ requires only
$Q>1/\xi=7500$; resolving $\xi^2$ would require $Q>5.6\times10^7$ \BM. Photonic
hardware thus reaches down to the $\xi$ floor, not below --- whereas NISQ
hardware, per Doc.~034, loses the $\xi$ corrections in noise. This is the only
place where $\xi$ ``plays a role in analysis'': not as the tool that finds the
resonance, but as the scale down to which anything can be distinguished at all.
$\xi$ has the same role everywhere in the corpus: $43200=(r_\mu/r_e)^2/\xi$
says \emph{where} the resonance is, not how to find it.

\textbf{Caveats \SM.} Whether the $100\xi$ level is actually subtractable is
asserted in the corpus, not carried out. Doc.~186 gives $Q>10^6$ for the optical
phase, not for mass ratios; the transfer $1/Q\to\varepsilon$ is an analogy. The
check of Doc.~186 itself (\texttt{pruef\_343e\_dok186\_check.py}) confirms the
TFLN quality but refutes its ``locking'' onto $\varphi^{-k}$ (contradiction to
Doc.~328 [K]), the Fibonacci condition for winding numbers ($r_\mu=16/5$,
$16\notin F$) and the B-meson interpretation ($\alpha=\xi$ would need
$9\times10^8$ events for $4\sigma$); Doc.~186 carries a corresponding
preliminary note since 3~September 2026.

\subsection{Consequence for factorisation: $N_\mathrm{max} \approx 1/\varepsilon$}

A resonance-based factoriser must separate the period $r$ from $r{+}1$
(resolution $1/r$), or, by the precision barrier of Doc.~075
($\Delta f \approx f_0/n^2$), isolate the factor $n$ (resolution $1/n^2$).
The resolution floor $\varepsilon$ therefore caps it at $N_\mathrm{max}\approx1/\varepsilon$ \BM:

\begin{table}[H]
\centering
\caption{Resolution floor and ceiling of a resonance-based factoriser}
\begin{tabular}{lrrrr}
\toprule
$\varepsilon$ & factors up to $n_\mathrm{max}$ & numbers up to $N_\mathrm{max}$ & bits \\
\midrule
$1.05\,\%$ (one fractal level) & 10 & 95 & 7 \\
$\xi$ & 87 & $7.5\times10^{3}$ & 13 \\
$1/Q_\mathrm{TFLN}$ & $10^3$ & $10^{6}$ & 20 \\
$\xi^2$ & $7.5\times10^3$ & $5.6\times10^{7}$ & 26 \\
RSA-2048 & --- & would need $\varepsilon\approx2^{-1024}$ & --- \\
\bottomrule
\end{tabular}
\end{table}

The $p^*\approx87$ of the threshold table \emph{is} the factorisation statement:
a resonance method at the $\xi$ floor separates factors up to about $87$. Even
with $\xi^2$ that is 26~bits --- trial division does it in milliseconds. What
moves with the $\xi$ floor is the Galois layer of physics ($k\approx3\to k\geq8$),
not factorisation: physics gains resolving power for mass ratios; number theory
gains nothing, because its numbers carry no $\xi$ scale (Doc.~075).

\subsection{Why classical methods reach further: state versus process}

Classical methods resolve nothing --- they count. $a^r \bmod N = 1$ is true or
false; there is no $\varepsilon$. Every bit is a $\mathbb{Z}_2$ state, in the
corpus a winding number on T$^4$ (Doc.~247): a \emph{state} that exists without
energy and is re-thresholded to $0$ or $1$ after every step. That is the
topological discreteness of Theorem~G$''$. A resonance method, by contrast,
carries its information as a \emph{process} --- phase, frequency --- and has no
state to threshold back to; the floor $\varepsilon$ stays with it. The price of
exactness is enumeration:

\begin{table}[H]
\centering
\caption{Classical limits are counting limits (operations), not resolution limits}
\begin{tabular}{rrrrl}
\toprule
bits & trial division $\sqrt N$ & Pollard-$\rho$ $N^{1/4}$ & NFS & \\
\midrule
13 & 90 & 10 & $9\times10^{2}$ & resonance ceiling at $\xi$ \\
26 & $8\times10^3$ & 90 & $3\times10^{4}$ & resonance ceiling at $\xi^2$ \\
512 & $10^{77}$ & $3\times10^{38}$ & $2\times10^{19}$ & hours \\
2048 & --- & $3\times10^{154}$ & $1.5\times10^{35}$ & $5\times10^{12}$ years at $10^{15}$ op/s \\
\bottomrule
\end{tabular}
\end{table}

On energy: Landauer's bound $k_BT\ln2$ holds in the corpus unchanged and without
$\xi$ modification (Doc.~184), but \emph{only for the erasure} of a bit
(Doc.~247); Doc.~255 derives it as the frozen energy of a minimal winding
difference $\Delta w=1$ from T$^4$ geometry. It caps dissipation per
irreversible step, not the number of steps; reversible steps are exempt. The
classical wall is therefore time, not energy \BM.

The two method types are dual: \textbf{exactness costs enumeration,
resolution costs precision.} Resonance has constant cost per candidate and a
floor at $1/\varepsilon$; counting has no floor and a cost growing
subexponentially in $N$. Both are limits; the counting limit merely lies
hundreds of orders of magnitude further out. Doc.~316 says it generally: the
arithmetic structure cannot be projected into a continuum without loss of
information.


\subsection{Quantum computers: the empirical yardstick}

Two levels must be separated. The \emph{circuit model} of Shor's algorithm is
digital and untouched by the Weyl obstruction (Theorem~F): the QFT runs over
$\mathbb{Z}_Q$, $Q=2^{2n}$, with equally spaced eigenfrequencies and linear
counting density $N(\lambda)\propto\lambda$; the number theory sits in the period
$r$ of $a^x \bmod N$, not in the spectrum \BM. That holds on paper.

The \emph{physical apparatus} is something else. A qubit is a two-level
resonator, every gate a resonant drive (Rabi rotation), a photon a mode with
continuous phase. Quantum computers and photonics do not compute digitally and
do not enumerate; they are resonant systems and are subject to the resonance
limits: the resolution floor $\varepsilon$ (Theorem~G$'$) and, wherever the
arithmetic is to be carried in a spectrum, the Weyl obstruction. That resonators
become ``digital'' is the claim of the threshold theorem (re-thresholding by
projective measurement at gate error $<10^{-3}$, approximate QFT with phase
resolution $1/\mathrm{poly}(n)$, connectivity, magic-state distillation,
modular exponentiation at $95\,\%$ of the gates, Doc.~147); each condition has
been demonstrated individually, the product at scale has not. Until it is, the
apparatus obeys what every resonator obeys. The substrate is immaterial ---
photonics, ions, superconductors differ only in the error channel (for TFLN,
photon loss: $<1$~dB per facet per Doc.~186 is up to $20\,\%$ per transition,
above the loss thresholds of fusion-based schemes). The photonic operations of
Doc.~186 (analogue matrix convolution, resonator locking) are analogue in any
case and sit, with $\varepsilon = 1/Q \approx 10^{-6}$, at
$N_\mathrm{max}\approx10^{6}$ (20~bit).

There are thus several limits --- resolution, Weyl, resources --- and the corpus
has no mechanism that frees a physical resonator from them (Doc.~034: $\xi$
effects below noise; Doc.~162: $100\xi$ damping per gate, character open).
Doc.~075 denies the obstruction for Shor; it is an older document, and its
denial holds for the model, not for the apparatus. Whether the threshold
theorem holds physically at scale is \SM\ and is decided not in theory but on
the factoring curve.

\begin{table}[H]
\centering
\caption{Factoring with Shor's algorithm versus classical records and building blocks (as of September 2026)}
\begin{tabular}{llll}
\toprule
Year & Shor on hardware & classical (NFS) & building blocks \\
\midrule
2001 & $N=15$ & --- & --- \\
2012 & $N=21$ (with foreknowledge) & --- & --- \\
2019 & attempt at $N=35$ failed & --- & --- \\
2020 & --- & RSA-250 (829~bit) & --- \\
2024 & --- & --- & error correction below threshold (Google Willow) \\
2025 & --- & --- & 48 logical qubits (Quantinuum Helios) \\
2026 & --- & --- & 94 logical qubits $<10^{-4}$; logical QFT (12 log.\ qubits) \\
\bottomrule
\end{tabular}
\end{table}

The larger ``quantum records'' ($56\,153$, $4\,088\,459$, $2.6\times10^{14}$,
$1.1\times10^{12}$) concern numbers of special structure (factors differing in
a few bits), solved by annealing or QAOA after classical pre-processing;
Gutmann and Neuhaus (IACR ePrint 2025/1237) replicated all of them with a 1981
VIC-20, an abacus and a dog. The largest \emph{simulated} Shor run (39~bit)
ran classically on 2048 GPUs.

The Shor curve has stood at $N=21$ since 2012; the classical curve at 829~bit.
The yardstick is therefore empirical: \textbf{an advantage must become visible
on the Shor factoring curve, not on component benchmarks.} That is the corpus
position of Doc.~075 --- \textbf{[X]} no general exponential quantum advantage
demonstrated --- and it remains valid in September 2026. The theoretical
possibility (Shor polynomial; Weyl does not apply; whether another physical
obstruction exists is open) remains \SM, not [X]; its burden of proof lies
with experiment. Resolution floor (G$'$), counting limit
(state/process) and resource limit (Shor) are three different ceilings; for
factoring only the third acts today, and it has not been broken.


% ============================================================
\section{Summary}
% ============================================================

\begin{table}[H]
\centering
\caption{Results and status}
\begin{tabular}{lll}
\toprule
Theorem & Content & Status \\
\midrule
A & $\zeta_{T^4/\mathbb{Z}_3}(s) = (1-3^{-s})\,\zeta(s) = L(s,\chi_0)$ & \BM \\
A$'$ & $L(s,\chi_1)$ measures twist-sector asymmetry & \SM \\
B & Euler product hierarchical by $k=\mathrm{ord}_p(3)$ & \BM \\
B$'$ & Fractal damping suppresses Euler factors $k\geq 7$ & \BM \\
C & Orbifold zeros at $s=2\pi ik/\ln 3$ on $\mathrm{Re}(s)=0$ & \BM \\
C$'$ & No resonance of Riemann zeros with Z$_3$ unit & \BM \\
D & Galois character does not separate; classes $=(\mathbb{Z}/26)^*/\langle 3\rangle\cong\mathbb{Z}_4$ & \BM \\
D$'$ & Sector pairing $k\leftrightarrow -k$: $f_1\leftrightarrow f_2$, $f_3\leftrightarrow f_4$ --- 4 classes $\to$ 2 & \BM \\
D$''$ & Negation couples order 26 to 13; no sub-lepton layer & \BM \\
D$'''$ & $\{f_1,f_2\}$ Majorana layer, $\{f_3,f_4\}$ Dirac layer (sector pairing $=$ Majorana criterion) & \BM \\
D$''''$ & $\nu_3$ Dirac (O4); $m_{ee}$ from $\nu_1,\nu_2$ only ($\leq$14.4 meV); IH more strongly excluded & \BM \\
E & $p=13$ unique prime with $\mathrm{ord}_p(3)=3$ worldwide; primes forced by $3^k-1$ & \BM \\
E$'$ & $\mathrm{ord}_p(3)=k$, $3|k$ $\Rightarrow$ $p\equiv 1\pmod 3$ (Z$_3$-compatible) & \BM \\
F & Euler factor threshold $p^*\approx 9.76$ ($s=2$) $=$ 7-limit boundary & \BM \\
F$'$ & Product density: chance level from $p_\mathrm{max}\approx 30$ ($\pm 0.5\,\%$) & \BM \\
F$''$ & Weyl obstruction: limit lies in the object, not the tool (Doc.~316) & \BM \\
G & Coupling: $Q_c=\pi/\sqrt{6\varepsilon}\approx 12.5$ at $1.05\,\%$; limit $k\approx3$--$4$ = critical point $\kappa=1$ & \BM \\
G$'$ & Same threefold division, different law: Arnold $K^q$ vs.\ Farey $1/q^2$ & \BM \\
G$''$ & FFGFT dynamically deeply subcritical ($K_\mathrm{eff}=2\pi\xi$); discreteness topological, not dynamical & \BM \\
G$'''$ & Theorem~F on $\mathbb{Z}/N\mathbb{Z}$ (RSA) only as analogy & \SM \\
H & $1.05\,\%$ (Doc.~338) $\approx 100\xi$ (Doc.~162): limit $k\approx3$ is that of one fractal level & \BM \\
H$'$ & At $\varepsilon=\xi$: $Q_c\approx111$, $p^*\approx87$, Galois resolved to $k\geq8$ & \BM \\
H$''$ & TFLN $Q>10^6$ resolves $\xi$, not $\xi^2$; $\xi$ = resolution floor, not selector & \BM \\
H$'''$ & Subtractability of the $100\xi$ level; Doc.~186: locking/Fibonacci/B-meson refuted & \SM \\
I & Resonance factoriser capped at $N_\mathrm{max}\approx1/\varepsilon$ (13--26 bit); classical: counting limit, Landauer erasure only & \BM \\
I$'$ & Circuit model digital (Weyl inapplicable); physical apparatus = resonator, subject to $\varepsilon$/Weyl until threshold theorem demonstrated \SM; curve at $N=21$ since 2012 & [X] \\
\bottomrule
\end{tabular}
\end{table}

The central statement: the full Riemann zeta $\zeta(s)$ belongs to the flat
$\mathbb{R}^4$ spectrum. The zeta of the T$^4$/Z$_3$ orbifold is
$L(s,\chi_0) = (1-3^{-s})\,\zeta(s)$ — a filtered version in which the
$3$-Euler factor is separated out and the primes are weighted by the Galois
hierarchy $k = \mathrm{ord}_p(3)$. This is the zeta-function form of the
entry rule of Doc.~342.

% ============================================================
\section*{Check scripts}
% ============================================================

Directory \texttt{2/python/Dok343\_Skripte/}, wrapper \texttt{run\_all\_343.sh}:
\begin{itemize}
  \item \texttt{pruef\_343\_zeta\_galois.py} ---
        Theorems~A--D; partial zeta decomposition, L-functions, zeros,
        resonance test, weighted Euler product.
  \item \texttt{pruef\_343b\_klassen\_symmetrie.py} ---
        Theorem~D; explicit Frobenius classes in GF$(27)^*$, action of inversion
        and negation, cosets of $\langle 3\rangle$ in $(\mathbb{Z}/26)^*$,
        exact values of $L(s,\chi_1)$.
  \item \texttt{pruef\_344\_primzahlmuster.py} ---
        Theorems~E--F; patterns in $\mathrm{ord}_p(3)$, exact threshold $p^*$,
        Galois vs.\ random hit rate, Euler factors.
  \item \texttt{pruef\_343c\_kopplung\_zahlen.py} ---
        Plain-terms~II section; Farey coverage and $Q_c(\varepsilon)$, law comparison
        Arnold $K^q$ vs.\ Farey $1/q^2$, regime of $K_\mathrm{eff}=2\pi\xi$,
        Galois coverage by $p_\mathrm{max}$.
  \item \texttt{pruef\_343f\_zweite\_schicht.py} --- Theorem~D$'''$; classes, orbits,
        involutions $k\mapsto-k$ and $k\mapsto k^{-1}$, Dirac/Majorana partition.
  \item \texttt{pruef\_343d\_xi\_aufloesung.py} --- Theorem~G$'$; threshold table for
        $\varepsilon\in\{1.05\,\%,\,100\xi,\,\xi,\,1/Q_\mathrm{TFLN},\,\xi^2\}$.
  \item \texttt{pruef\_343e\_dok186\_check.py} --- check of Doc.~186: TFLN quality,
        Arnold tongues at Fibonacci approximants, 50-GHz ladder, B-meson statistics,
        winding numbers vs.\ Fibonacci.
\end{itemize}

All assertions passed.

% ============================================================
\section*{Note on the use of AI assistance}
% ============================================================

This document was prepared with the assistance of Claude (Anthropic).
All numerical computations are verified by Python check scripts.
The physical interpretations and conclusions are those of Johann Pascher
(ORCID 0009-0000-6518-4064).

\begin{thebibliography}{9}
\bibitem{P057} J.~Pascher, \textit{Doc.~057: Relational number system}, FFGFT corpus.
\bibitem{P159} J.~Pascher, \textit{Doc.~159: Harmonic structure of the torus}, FFGFT corpus.
\bibitem{P034} J.~Pascher, \textit{Doc.~034: T0 formalism and QM optimisation}, FFGFT corpus.
\bibitem{P162} J.~Pascher, \textit{Doc.~162: T0 optimisation strategies compared}, FFGFT corpus.
\bibitem{P186} J.~Pascher, \textit{Doc.~186: FFGFT photonics analysis}, FFGFT corpus (with preliminary note of 3~September 2026).
\bibitem{P184} J.~Pascher, \textit{Doc.~184: The p-bit in FFGFT}, FFGFT corpus.
\bibitem{P247} J.~Pascher, \textit{Doc.~247: Information as state and as process}, FFGFT corpus.
\bibitem{P255} J.~Pascher, \textit{Doc.~255: Information kinetics --- Landauer and Vopson from T$^4$ geometry}, FFGFT corpus.
\bibitem{P328} J.~Pascher, \textit{Doc.~328: Coupling regimes, resonance and synchronisation}, FFGFT corpus, August~2026.
\bibitem{P330} J.~Pascher, \textit{Doc.~330: Three operations from T$^4$ to the observation space}, FFGFT corpus, August 2026.
\bibitem{P336} J.~Pascher, \textit{Doc.~336: The algebraic bridge: FFGFT and $\mathbb{Z}_3\mathbb{C}$}, FFGFT corpus, August 2026.
\bibitem{P075} J.~Pascher, \textit{Doc.~075: RSA and period-finding}, FFGFT corpus, August~2026.
\bibitem{P147} J.~Pascher, \textit{Doc.~147: Quantum computing and T0 time-mass duality}, FFGFT corpus, August~2026.
\bibitem{P316} J.~Pascher, \textit{Doc.~316: FFGFT and the Riemann zeta function}, FFGFT corpus, August~2026.
\bibitem{P342} J.~Pascher, \textit{Doc.~342: Factorisation classes in G$(6)$ and the harmonics of the primes}, FFGFT corpus, 2~September 2026.
\bibitem{Gutmann2025} P.~Gutmann, S.~Neuhaus, \textit{Replication of Quantum Factorisation Records with an 8-bit Home Computer, an Abacus, and a Dog}, IACR ePrint 2025/1237.
\bibitem{Gidney2019} C.~Gidney, M.~Ekerå, \textit{How to factor 2048 bit RSA integers in 8 hours using 20 million noisy qubits}, arXiv:1905.09749; C.~Gidney (2025), resource estimate $<10^6$ qubits.
\bibitem{Dirichlet} P.\,G.\,L.~Dirichlet, \textit{Über die Bestimmung der Dichte einer unendlichen Anzahl von Punkten}, 1837.
\end{thebibliography}
